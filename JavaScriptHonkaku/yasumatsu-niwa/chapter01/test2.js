console.log('Live Serverで実行');

/**
 * この関数は2つの数値を引数として取り、その和を返す関数 
 * @param {number} x 
 * @param {number} y 
 * @returns xとyの和
 */
function add(x, y) {
    return a + b;
}

const x = 10;
const y = 20;
const z = add(x, y); 