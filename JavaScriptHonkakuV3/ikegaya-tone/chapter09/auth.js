document.addEventListener('DOMContentLoaded', () => {
    const auth_button = document.querySelector('#auth-button');
    auth_button.disabled = true;
    /*
    ユーザ名、パスワードの両方が8文字以上の場合だけ、認証ボタンをクリックできるようにする
    */
});