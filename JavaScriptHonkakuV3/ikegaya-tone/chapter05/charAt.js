const name = "川久保智晴";

for (let i = 0; i < name.length; i++) {
    console.log(`文字 ${name.charAt(i)} の文字コードは ${name.charCodeAt(i)}`); // エスケープ文字列
    // console.log('文字 ' + name.charAt(i) + ' の文字コードは ' + name.charCodeAt());
}