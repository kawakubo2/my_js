const score = 70;

if (score >= 60) {
    console.log('合格');
} else {
    console.log('不合格');
}

// 条件演算子
console.log(score >= 60 ? '合格': '不合格');

const judge = score >= 60 ? '合格': '不合格';
console.log(judge);